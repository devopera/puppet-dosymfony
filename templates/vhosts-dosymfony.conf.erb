#
#  Devopera Symfony vhosts
#    - dynamically generated by puppet
#

<Directory "<%= @target_path %>/<%= @title %>/web/">
    Options Indexes MultiViews FollowSymLinks
    AllowOverride All
    Order allow,deny
    Allow from all
</Directory>

#  First vhost is default, so picks up ip_address requests
<VirtualHost *:80>
    ServerAdmin admin@example.com
    DocumentRoot "<%= @target_path %>/<%= @title %>/web/"
    ServerName  <%= @app_name %>.localhost
    ServerAlias <%= @app_name %>.<%=hostname%>
    ServerAlias symfony.localhost
    ServerAlias symfony.<%=hostname%>
    ErrorLog logs/<%= @app_name %>-error_log
    CustomLog logs/<%= @app_name %>-access_log common
</VirtualHost>
